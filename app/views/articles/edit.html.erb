<%= form_for(@article) do |element| %>
  <%= element.label :title %>
  <%= element.text_field :title %>

  <%= element.label :content %>
  <%= element.text_field :content %>

  <%= element.submit %>

<% end %>

<%= link_to "Back to item", "/articles/#{@article.id}" %>